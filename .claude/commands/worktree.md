---
description: Show the current TaskNode recursion tree as an ASCII diagram
---

Call the `harness_render_task_tree` tool and print its output verbatim inside a fenced code block. Do not add commentary beyond the diagram itself.
